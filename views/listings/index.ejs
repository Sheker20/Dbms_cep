<% layout("/layouts/boilerplate.ejs") %>
<body>
    <h3>
      <!-- Check your travel listings here -->
       Order your products here 
      <br>
    </h3>
    <div class="row ">
      <% for(let listing of allListings) { %>
        <!-- <a href="/listings/<%= listing._id %>" class="listing-link">
          <div class="card col">
            <img 
              src="<%= listing.image %>" 
              class="card-img-top" 
              alt="listing_img" 
              style="height: 20rem;">
              <div class="card-img-overlay"></div>
            <div class="card-body">
              <p class="card-text">
                <b><%= listing.title %></b> 
                <br>
                <b>&#8377;<%= listing.price.toLocaleString("en-IN") %></b> onwards
              </p>
            </div>
          </div>
        </a> -->

        <a href="/listings/<%= listing._id %>" class="listing-link">
          <div class="card col">
            <img 
              src="<%= listing.image %>" 
              class="card-img-top" 
              alt="listing_img"
              style="height: 20rem;">
            <div class="card-body">
              <h5 class="card-title"><%= listing.title %></h5>
              <p class="card-text">
                <b><%= listing.title %></b> 
                <br>
                <b>&#8377;<%= listing.price.toLocaleString("en-IN") %></b> onwards
              </p>
              <a href="" class="btn btn-primary">Add to cart</a>
            </div>
          </div>
        </a>
        <% } %>
    </div>
  </body>
